# Prompt 2: Archive GitHub

Use this at the end of an important conversation.

```markdown
Archive GitHub
```

Long form if needed:

```markdown
Archive GitHub

Save the important durable context from this chat into my private AI You memory repo.

Use `skills/archive-github/SKILL.md`.

Do not write personal notes to the public `utb2017/ai-you` repo.

Choose the right folder, create structured Markdown notes, update indexes/logs, redact dangerous secrets, and verify the write by reading the repo afterward.

If GitHub write actions are not visible, search/list/discover the GitHub connector/app action surface first. Look for create file, update file, repository contents, save file, commit file, create private GitHub file, or create/update file contents.

If this session still cannot write, produce a complete archive packet with full file bodies and exact index/log updates so the memory can still be saved.
```
