# Logs

This folder tracks imports, setup events, and workflow notes.

Keep `import-log.md` updated whenever a chat is archived.

Use `smoke-tests/` for setup verification files that prove ChatGPT can write to the private repo.
