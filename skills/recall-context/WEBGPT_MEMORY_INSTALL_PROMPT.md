# WebGPT Memory Install Prompt For Recall Context

```markdown
Please remember this as a durable AI You command:

Command: `Recall Context`

When I say `Recall Context`, `pull context`, `check my memory`, `check GitHub memory`, `look up my relationship`, `pull my relationship data`, `pull my work context`, or `before you answer, check my GitHub`, use my private AI You memory repo before answering.

Default private repo name:
`ai-you-memory`

Before retrieving, read:
`skills/recall-context/SKILL.md`

Important:

- This is a retrieval task, not an archive task.
- Do not ask me to explain background if the repo contains it.
- Search by category, names, projects, and recent imports.
- Pull only relevant context.
- Answer with continuity, not a generic reset.
- Never read or write my personal notes from the public foundation repo `utb2017/ai-you`.

If memory is available, save this command.
```

